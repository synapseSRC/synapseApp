# Build configuration
projectVersion=0.1.3
android.suppressUnsupportedCompileSdk=36
android.enableR8.fullMode=false
android.enableJetifier=true
android.useAndroidX=true

# Performance
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.daemon=true

# Kotlin
kotlin.code.style=official
kapt.include.compile.classpath=false

# Supabase Configuration (replace with your actual values)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# Gemini AI Configuration
GEMINI_API_KEY=your-gemini-api-key-here

# Authentication Configuration
# Set to false to disable email verification in development
auth.require.email.verification=true
# Set to true to enable debug logging
auth.enable.debug.logging=false
# Set to true to enable development mode
auth.development.mode=false
# Resend verification email cooldown in seconds
auth.resend.cooldown.seconds=30
# Maximum resend attempts
auth.max.resend.attempts=5
# Auto retry attempts for network errors
auth.auto.retry.attempts=3
# Retry delay in milliseconds
auth.retry.delay.ms=1000
# KSP2 Configuration
ksp.useKSP2=true

# Google OAuth Configuration
# Replace with your actual Web Client ID from Google Cloud Console
# Format: xxxxx-xxxxx.apps.googleusercontent.com
GOOGLE_WEB_CLIENT_ID=your-google-web-client-id-here

# OneSignal Configuration (credentials moved from source code)
ONESIGNAL_APP_ID=044e1911-6911-4871-95f9-d60003002fe2
ONESIGNAL_REST_API_KEY=YOUR_ONESIGNAL_REST_API_KEY_HERE
kotlin.native.ignoreDisabledTargets=true
